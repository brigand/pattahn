---
name: And (condition)
route: /cond/And
menu: Conditions
---

```js
import match from 'matchany';
import And from 'matchany/cond/And';

const VALUE = 1;

const result = match()
  .with(And(), 'matches')
  .any('nope')
  .exec(VALUE);

expect(result).toBe('matches');
```
