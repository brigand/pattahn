---
name: AndThen (condition)
route: /cond/AndThen
menu: Conditions
---

```js
import match from 'matchany';
import AndThen from 'matchany/cond/AndThen';

const VALUE = 1;

const result = match()
  .with(AndThen(), 'matches')
  .any('nope')
  .exec(VALUE);

expect(result).toBe('matches');
```
