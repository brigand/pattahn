---
name: Id (condition)
route: /cond/Id
menu: Conditions
---

```js
import match from 'matchany';
import Id from 'matchany/cond/Id';

const VALUE = 1;

const result = match()
  .with(Id(), 'matches')
  .any('nope')
  .exec(VALUE);

expect(result).toBe('matches');
```
