---
name: Id (condition)
route: /cond/Id
menu: Conditions
---

# Id (condition)

This is the identity function, which simply returns an equivalent match to the input.

```js
import Id from 'pattahn/cond/Id';

const VALUE = 7;

expect(Id().exec(VALUE)).toEqual([VALUE]);
```
