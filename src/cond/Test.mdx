---
name: Test (condition)
route: /cond/Test
menu: Conditions
---

```js
import match from 'matchany';
import Test from 'matchany/cond/Test';

const VALUE = 1;

const result = match()
  .with(Test(), 'matches')
  .any('nope')
  .exec(VALUE);

expect(result).toBe('matches');
```
