---
name: Prop (condition)
route: /cond/Prop
menu: Conditions
---

```js
import match from 'matchany';
import Prop from 'matchany/cond/Prop';

const VALUE = 1;

const result = match()
  .with(Prop(), 'matches')
  .any('nope')
  .exec(VALUE);

expect(result).toBe('matches');
```
