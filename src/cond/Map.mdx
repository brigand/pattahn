---
name: Map (condition)
route: /cond/Map
menu: Conditions
---

```js
import match from 'matchany';
import Map from 'matchany/cond/Map';

const VALUE = 1;

const result = match()
  .with(Map(), 'matches')
  .any('nope')
  .exec(VALUE);

expect(result).toBe('matches');
```
