---
name: Or (condition)
route: /cond/Or
menu: Conditions
---

```js
import match from 'matchany';
import Or from 'matchany/cond/Or';

const VALUE = 1;

const result = match()
  .with(Or(), 'matches')
  .any('nope')
  .exec(VALUE);

expect(result).toBe('matches');
```
